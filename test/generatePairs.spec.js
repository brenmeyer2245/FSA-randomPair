const { assert, expect } = require("chai");
const {
  createPairs,
  selectPair,
  getNewExistingPairs,
  randomNumber,
  extractStudentIds,
  checkPairComboAvailability,
} = require("./test-modules/generate-pairs");

describe("generatePairs", () => {
  describe("randomNumber", () => {
    it("returns a number", () => {});
    it("returns a number within the range", () => {});
  });

  describe("updatePairTracker", () => {
    it("mutates the input tracker", () => {});
    it("adds the new pair combos to the tracker", () => {});
  });

  describe("updatePairTracker", () => {
    it("mutates the input tracker", () => {});
    it("adds the new pair combos to the tracker", () => {});
  });
  describe("checkPairComboAvailability", () => {
    it("returns a boolean whether the pair is in the tracker", () => {});
  });
  describe("generatePairIdCombo", () => {
    it("returns a sorted array of ids", () => {});
    it("only uses ids from the available ids input", () => {});
  });
});
